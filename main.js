const one = 1;
const two = 2;

const sum = (num1, num2) => {
  return num1 + num2;
}

const multiple = (num1, num2) => {
  return num1 + num2;
}

console.log(one, two);
console.log(two, one);
console.log(sum(one, two));